Build docker image

docker build --no-cache -t rag-chatbot .

docker run -p 8501:8501 -p 8000:8000 rag-chatbot

#stop container
docker stop container_id

#start the stop container
docker start container_ID

#SEE ALL
docker
#see all at once
docker ps -a

#to remove
docker rm container_id

#delete a image
docker rmi image_id

docker stop $(docker ps -q)

docker rm $(docker ps -aq)



